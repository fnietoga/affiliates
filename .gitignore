# General
.DS_Store
*.swp

# Rider
.idea/
*.sln.iml

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Node.js / React (frontend)
frontend/node_modules/
frontend/build/
frontend/coverage/
frontend/.pnp
frontend/.pnp.js
frontend/.env.local
frontend/.env.development.local
frontend/.env.test.local
frontend/.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# .NET / ASP.NET Core (backend)
backend/bin/
backend/obj/
backend/**/Properties/launchSettings.json
*.user
*.suo

# Terraform (iac)
# These are already in iac/terraform/.gitignore, but it's good to have them here as well just in case.
# iac/terraform/.terraform/
# iac/terraform/*.tfstate
# iac/terraform/*.tfstate.*
# iac/terraform/crash.log
# iac/terraform/crash.*.log
# iac/terraform/*.tfvars
# iac/terraform/*.tfvars.json
# iac/terraform/override.tf
# iac/terraform/override.tf.json
# iac/terraform/*_override.tf
# iac/terraform/*_override.tf.json
# iac/terraform/.terraformrc
# iac/terraform/terraform.rc
# iac/terraform/*.tfplan

# Flyway (backend/db)
# There are usually no files generated by Flyway that need to be globally ignored,
# since SQL scripts are part of version control.
# The flyway.conf could contain secrets if environment variables are not used,
# but it's already in the project. It's recommended to use environment variables for secrets.

# Logs
logs
*.log

# Archivos de sistema operativo
Thumbs.db
.Spotlight-V100
.Trashes
ehthumbs.db
Desktop.ini
